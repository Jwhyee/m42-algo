package COCI06

import java.util.LinkedList

fun main() = with(System.`in`.bufferedReader()) {

   val letters = readLine()
   val size = letters.length * 4 + 1

   for (i in 0 until letters.length) {

   }


   println(size)


   Unit
}